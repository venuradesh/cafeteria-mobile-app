import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileClient = () => {
  return (
    <View>
      <Text>ProfileClient</Text>
    </View>
  );
};

export default ProfileClient;

const styles = StyleSheet.create({});
